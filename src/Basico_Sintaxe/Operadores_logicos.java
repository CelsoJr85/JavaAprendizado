package Basico_Sintaxe;

public class Operadores_logicos {
    // Operadores Relacionais:
    // == igual
    // != diferente de
    // > maior quê
    // >= maior quê ou igual a
    // < menor quê
    // <= menor quê ou igual a


    // Operadores Logicos:
    // &&  and(e)  falso mesmo que só um, tudo fica falso;
    // ||  or(ou)  verdadeiro, mesmo que só um, tudo fica verdadeiro;
    // ^ xor       quando um é == outro, é verdadeiro; já diferente é falso
    // ! inversão  inverte o anterior

    // Operadores Shift:
    // ~, >>, >>>, <<   alterar zeros a esquerda ou direita





}
